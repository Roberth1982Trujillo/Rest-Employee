spring:
  application:
    name: REST-Employee
  config:
    import:
      optional:configserver:http://localhost:8083
  RabbitMQ:
    host: localhost
    port: 5672

server:
  port: 9002

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/